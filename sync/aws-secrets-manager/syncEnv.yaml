imports:
  - dev-sandbox
values:
  sync:
    awsSecretsManager:
      value:
        myConfigKey: myImportantValue
        myNestedKey:
          haha: business
        mySecret: ${aws.login.accessKeyId}
      name: name-in-secrets-manager

