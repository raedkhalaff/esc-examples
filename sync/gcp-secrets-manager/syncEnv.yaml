imports:
  - dev-sandbox
values:
  sync:
    gcpSecretManager:
      value:
        myConfigKey: myImportantValue
        myNestedKey:
          haha: business
        mySecret: ${gcp.auth.accessToken}
      name: my-gcp-secret
