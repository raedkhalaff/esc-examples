name: esc-sync-demo-aws-parameter-store
runtime: nodejs
description: A pulumi program that creates a stack with deployment settings and a scheduled update to run every hour to sync ESC values with AWS Parameter Store.
config:
  pulumi:tags:
    value:
      pulumi:template: aws-typescript
